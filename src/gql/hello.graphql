query hello (
  $page: Int
  $currentSize: Int
) {
  hello (
    page: $page
    currentSize: $currentSize
  ){
    page
    total
    total_page
    list {
      id
      audit_status
    }
  }
}
